hl.window_rule({
    name  = "suppress-maximize-events",
    match = { class = ".*" },
    suppress_event = "maximize",
})

hl.window_rule({
    name  = "fix-xwayland-drags",
    match = {
        class      = "^$",
        title      = "^$",
        xwayland   = true,
        float      = true,
        fullscreen = false,
        pin        = false,
    },
    no_focus = true,
})

-- Remove border and gaps when only one tiled window is open
hl.workspace_rule({ workspace = "w[t1]", gaps_out = 0, gaps_in = 0, border_size = 0 })
